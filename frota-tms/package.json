{
  "name": "frota-tms",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de Roteirização e Gestão de Frota para Carregamentos",
  "scripts": {
    "dev": "concurrently -n api,web -c teal,cyan \"npm run dev --prefix api\" \"npm run dev --prefix web\"",
    "dev:api": "npm run dev --prefix api",
    "dev:web": "npm run dev --prefix web",
    "build": "npm run build --prefix api && npm run build --prefix web",
    "db:seed": "npm run db:seed --prefix api",
    "db:migrate": "npm run db:migrate --prefix api",
    "setup": "npm install --prefix api && npm install --prefix web && npm run db:push --prefix api && npm run db:seed --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
